import Button from "./Button";

const FollowButton = () => {
  return (
    <Button className="text-white bg-black font-bold hover:bg-gray-800">
      Follow
    </Button>
  );
};

export default FollowButton;
